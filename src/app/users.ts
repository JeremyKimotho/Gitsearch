export class User {
  constructor(public login:string, public bio:string, public public_repos:number, public followers:number, public following:number, public avatar_url:any, public html_url:any, public created_at:any, public repos_url:any){}
}
