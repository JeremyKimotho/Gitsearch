import { DaysSinceCreationPipe } from './days-since-creation.pipe';

describe('DaysSinceCreationPipe', () => {
  it('create an instance', () => {
    const pipe = new DaysSinceCreationPipe();
    expect(pipe).toBeTruthy();
  });
});
